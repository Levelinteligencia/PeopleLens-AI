# Contrato do KPI `time_to_hire` (ADR-0011).
#
# `depends_on_checks` NAO e digitado: e derivado de `consumed_by_kpis` no
# catalogo de checks, e o teste test_contrato_de_kpi_bate_com_o_catalogo
# reprova se as duas pontas divergirem.
#
# `minimum_n` e a supressao do ADR-0007: abaixo dele o recorte nao responde,
# e a recusa e por privacidade, nao por qualidade.
#
# --- F7 ---------------------------------------------------------------------
# `status` e `certification` NAO sao calculaveis a partir do trust score
# (ADR-0029). CERTIFIED e aprovacao humana registrada, com dono e data.
# Promover porque o dado melhorou e o mesmo erro que a F4 proibe na promocao
# automatica de mapeamento.
#
# `interpretation_rules` vazio significa INTERPRETACAO BLOQUEADA. Nao preencher
# para destravar o nivel: sem banda ou meta assinada, nao ha o que interpretar.
kpi_id: time_to_hire
name: Tempo para contratar
description: Dias entre candidatura e admissao.
business_definition: Mediana de dias entre a candidatura e a admissao, entre as candidaturas
  que resultaram em admissao.
formula: mediana de (data de admissao menos data de candidatura)
grain: trimestre x business unit
population: candidaturas com admissao registrada
period_grain: trimestre
period_rollup: soma
period_coverage:
  from: 2020-Q4
  to: 2026-Q2
allowed_dimensions:
- business_unit
- departamento
- nivel
filters:
- regra: date_sk_hire > 0
  porque: marco nao atingido nao tem duracao
exclusions:
- o_que: candidaturas sem admissao
  porque: nao ha tempo a medir
source_tables:
- dim_calendar
- dim_job_level
- dim_organization
- fact_application
- fact_requisition
coverage: null
owner: people-analytics
version: 1.0.0
status: DECLARED
certification:
  approved_by: null
  approved_at: null
  blockers:
  - motivo: fora do escopo de certificacao da v1.0
    resolvido_por: decisao de People Analytics
depends_on_checks:
- DQ_COMP_013
- DQ_CONS_007
- DQ_CONS_011
- DQ_RECON_003
- DQ_REF_010
- DQ_TIME_006
- DQ_VALID_014
depends_on_mappings:
- ref_department_mapping
minimum_n: 5
response_levels:
  FATO: liberado
  CONTEXTO: liberado; associacao e sempre nomeada como associacao
  INTERPRETACAO: bloqueado ate haver regra registrada no contrato, com dono
  CAUSALIDADE: negado por padrao (ADR-0015); sem estudo registrado nao ha resposta causal
interpretation_rules: []
causal_studies: []
